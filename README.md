# normalization_text
L'objectif principal de ce test est de développer un système de normalisation de texte basé sur un transducteur à états finis (FST) qui atteigne le taux d'erreur de mots (WER) le plus bas possible sur un ensemble de test à l'aveugle pour des nombres de 0 à 1000
